name: ai_safeguard
description: An intelligent emergency response application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.3
  telephony: ^0.2.0
  shared_preferences: ^2.2.1
  speech_to_text: ^7.4.0
  permission_handler: ^11.4.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
